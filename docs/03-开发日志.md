# 校园乐跑项目开发日志

## 当前目标

从零搭建一个可交付答辩的校园乐跑系统，包含小程序端、后端、管理端和完整文档。

## 当前完成度

- 已完成：阅读并拆解 `duty.md`
- 已完成：明确项目采用三端架构
- 已完成：产出任务清单与开发文档初稿
- 已完成：后端 Spring Boot 工程、数据库迁移、演示数据、JWT 鉴权、核心业务接口
- 已完成：Vue 管理后台工程与主要页面
- 已完成：微信小程序工程与核心页面
- 已完成：Docker Compose、README、部署说明、项目说明
- 已完成：本地依赖启动、后端运行验证、关键接口验证
- 已完成：可复现启动脚本与小程序配置外置
- 已完成：后端与管理端容器化，Compose 全栈一键启动
- 已完成：全栈 Docker 实启验证，容器健康检查修正
- 进行中：微信开发者工具实机导入验证与后续细节优化

## 当前结论

- 采用模块化单体后端，保证开发效率与稳定性
- 采用 MySQL + Redis + Docker Compose 作为基础环境
- 小程序端采用原生微信小程序目录结构，确保可直接导入微信开发者工具
- 管理端采用 Vue 3 + Vite，兼顾开发效率与展示效果

## 接下来要做

1. 使用微信开发者工具导入 `miniapp/` 做页面与交互核验
2. 继续确认 Windows 微信开发者工具到 WSL Docker 后端的请求链路稳定性
3. 如需继续完善，可增加真实 GPS 轨迹采集与微信授权登录
4. 可继续补充更多管理员二级功能和图表展示

## 风险与处理

- 微信开放能力无法在当前终端直接真机调试
  - 处理：完成符合微信开发者工具导入规范的小程序工程，并提供演示模式与调试说明
- 题目原文提及微服务，实际实现采用单体
  - 处理：在开发文档中明确这是答辩版工程化取舍，并保留后续拆分路径
- 环境依赖可能缺失
  - 处理：优先通过 Docker Compose 补齐服务，缺少前端依赖时允许安装
- 本机 `6379` 端口已被占用
  - 处理：项目 Redis 容器端口映射改为 `6380`，后端默认配置已同步
- 小程序端当前未在微信开发者工具内实际截图验证
  - 处理：已按原生项目结构搭建，后续需要在开发者工具中导入检查页面渲染与本地接口联通
- 小程序在 Windows 微信开发者工具 + WSL 后端场景下存在地址差异
  - 处理：已新增 `scripts/configure-miniapp-api.sh` 自动生成 `miniapp/config.js`
- 目前宿主机软件版本差异已不是主风险
  - 处理：后端与管理端已容器化，启动优先使用 `docker compose up -d --build`
- 当前宿主机缺少 `docker buildx` 插件
  - 处理：`scripts/dev-up.sh` 已改为兼容模式，使用 `DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0`
- 管理端容器最初健康检查失败
  - 处理：将健康检查地址由 `localhost` 调整为 `127.0.0.1`，现已恢复正常

## 本轮验证记录

- 已执行 `docker compose up -d`，MySQL 与 Redis 容器健康
- 已执行 `mvn -Dmaven.repo.local=/tmp/m2repo test -q`，测试通过
- 已执行后端启动验证，Flyway 迁移成功，应用可正常启动
- 已执行 `./scripts/dev-up.sh`，MySQL、Redis、Backend、Admin 全部容器可启动
- 已验证 `campus-run-backend`、`campus-run-admin` 容器健康检查通过
- 已验证接口：
  - `GET /api/public/home`
  - `POST /api/auth/login`
  - `GET /api/gamification/overview`
  - `GET /api/user/me`
- 已验证页面：
  - `http://127.0.0.1:5173`
- 已修复：学生登录后首页 `overview` 接口 500
  - 原因：`GamificationService` 中 `Map.of(...)` 遇到 `completedAt = null` 触发异常
  - 处理：改为 `LinkedHashMap` 组装响应，并为全局异常处理补日志与兜底消息

## 交接约定

后续每次继续开发时，优先阅读本日志，再阅读 `docs/01-任务清单.md` 和 `docs/02-开发文档.md`，即可快速恢复上下文。
